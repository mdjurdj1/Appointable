<fieldset>
  <legend><%= contact.name %></legend>
</fieldset>
  <p><strong>Email: </strong><%= contact.email %></p>
  <p><strong>Phone Number: </strong><%= contact.phone_number %></p>
  <p><%= link_to "Edit", edit_contact_path(contact) %> | <%= link_to "Delete", contact_path(contact), method: :delete, data: { confirm: "Are you sure you want to delete this appointment?" } %></p>
   <br />
