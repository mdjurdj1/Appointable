<div class="boxed">
<fieldset>
  <legend><%= location.name %></legend>
</fieldset>

  <% if location.has_upcoming_appointments? %>
    <button type="button" id="showModal" class="btn btn-primary" data-toggle="modal" data-target="#appointmentModal" data-id="<%=location.id%>">View Appointments For This Loation</button> <br /><br />
    <p><%= link_to "Click to view upcoming appointments for this location.", location_appointments_path(location) %></p>
  <% end %>

  <% if has_empty_fields?(location) %>
    <p>This location has incomplete data! Update it by selecting 'Edit'.</p>
  <% else %>
    <p><%= location.street_address %></p>
    <p><%= location.city %>, <%= location.state %>, <%= location.zip_code %></p>
  <% end %>
  <p><%= link_to "Edit", edit_location_path(location) %> | <%= link_to "Delete", location_path(location), method: :delete, data: { confirm: "Are you sure you want to delete this location?" } %></p>

  <div class="modal fade" id="appointmentModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel"><%= location.name %></h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          HELLO HELLO HELLO
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
          <button type="button" class="btn btn-primary">Save changes</button>
        </div>
      </div>
    </div>
  </div>


</div>
